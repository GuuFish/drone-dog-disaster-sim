# 公里级仿真世界比例约定

- 水平范围：x ∈ [-3400, 3400] m，z ∈ [-2600, 2600] m，面积 35.36 km²。
- 数据、协议坐标、距离、速度与寻路全部使用米；Three.js 内部统一除以 `WORLD.renderScale = 100`。
- 当前不使用垂直夸张，水平、垂直、设备模型与自由视角保持同一比例。
- 自由视角从切换前的相机位置和朝向进入，最低离地高度为 1.7 m；默认移动速度 30 m/s，可选 10 / 30 / 100 m/s。
- 设备 `y` 是绝对海拔。停靠无人机约为 `surfaceHeight + 0.35 m`，机器狗模型中心约为 `surfaceHeight + 0.82 m`；飞行时无人机为 `surfaceHeight + cruise`。
- `surfaceHeight(x,z)` 取地形、建筑屋顶和桥面中的最高实体表面。无人机必须高于它，地面寻路则避开建筑占地。
- 世界地形包含连续山脊、沟谷、河谷、平原和集结中心平整区。地形显示、设备贴地和 A* 坡度判断都调用 `terrain.js`。
- 地面 A* 网格为 40 m，拒绝坡度大于 0.45 的相邻格；河流只允许在桥梁附近通过，任务可在目标 60 m 内的可通行点完成抵近。
- 无人机速度为 18 m/s，机器狗速度为 2.2 m/s，搜索半径为 300 m。倍速仅加快仿真时钟，内部最大 2 秒子步进。
- 无人机、机器狗和车辆按近似真实尺寸显示。公里级全景下设备本来就很小，因此使用标签辅助辨认，不再随相机距离放大模型。
- 浏览器存档键为 `sky-v4`；旧的 `sky-v3` 数据不会读取，避免设备停靠位、状态和协议事件结构混用。
- 地图为程序生成的演示场景，不是真实 GIS、卫星地形、真实传感器或经过验证的续航模型。
